# 第二次作业

要求：按照设计完成页面

## 原型设计

列表页：筛选区、导航栏、展示区（排序 + 简略图 + 分页）
详细页：导航栏、主图轮播区、信息与规则选择区、推荐区

## 列表页业务逻辑分析（数据流向）

1. 用户点击筛选
2. 筛选条件更改后，触发回调
3. 回调影响物品展示部分，所以筛选条件应该选择Props（由页面传递）
4. 而展示区，可以分为排序、简略图、分页
5. 点击展示区内部排序，会影响简略图的渲染顺序，所以选择Props（由展示区传递）
6. 简略图，本身不会保存状态，只是展示，选择props
7. 分页，分页策略会影响简略图的渲染数量，所以选择props

Page：商品列表，由于只在本身展示，所以这个可以是state
筛选区组件：通过props改变商品列表（由Page传入筛选条件和筛选的回调函数（不能直接传入商品的setState））
展示区组件：通过props传入商品列表，排序不需要由page传入，因为排序只会影响到展示区组件，所以内部写排序和分页的配置，分页数量应该由state维护
排序组件：由展示区传入props（排序回调）
分页组件：由展示区传入配置，主要是当前商品列表数量
缩略图组件：根据分页来批次渲染，同时由商品列表决定渲染内容

**AI评价** ： 将所有会影响其他组件的状态统一放置在父组件中，也就是
Page：商品、过滤、分类、分页等
父组件负责业务逻辑，子组件负责UI渲染

上面好像忘记了搜索栏，也就是导航栏，这个也该放在Layout中进行处理

## 详情页业务逻辑分析

1. 首先回看导航栏，放在Layout里面，负责URL跳转等操作（搜索请求回调处理，有个问题，那么这个列表是不是放在Zustand里面可能更好）
2. 对于Page，里面应该有物品的详细状态，使用State即可
3. 轮播图和缩略图一样，都应该传入props
4. 相似商品推荐，也是展示，应该同时传入跳转回调
5. 信息与规格筛选，这里的信息应该是跟随商品一起传进来的，需要注意的是，里面有加入购物车的行为，点击后，应该使用zustand改变全局状态，这个与购物车相关的了。

Page：商品的详细信息，加入购物车的回调，链接跳转回调？（把跳转作为推荐物品本身的属性还是传入的props？）
主图轮播区：props，由Page传入
信息与规格选择区：信息，由Page传入，不过，对于像选中尺码和颜色，这些应该存在Page的state里面吧，而像购买数量，也放在Page，以及触发的回调，也应该在Page里

**AI评价**：设计是对的，Page部分尤其是。

## 导航栏业务逻辑分析

这部分看着简单其实并非，特别是搜索部分，需要明确这部分的搜索逻辑

1. 用户输入关键词，导航栏搜索框改变state
2. 点击搜索，导航栏处理
3. 路由跳转，跳转到搜索URL
4. 执行搜索API，显示搜索结果
5. 更新页面状态，搜索结果和加载状态
6. 同步URL，更新浏览器地址栏

也就是说，我们可以将列表页作为动态路由页面，接受URL里面的参数，根据参数向后端发起请求，然后将结果保存在Page状态里面，分发给各个子组件进行渲染。
需要注意的是，我们的筛选，实际上也应该是一个路由跳转，也应该是一个发送给后端的请求参数，保存在URL里面。

## 组件设计

我只是把我的上面的分析简单写了个结构发给ai叫他补充后，他就生成了下面的一大堆。。。
我觉得不赖，只是有些功能属于暂时不需要的，可以后面加上去.
顺便还去淘宝调研了一波，看看他们的urlParams是怎么样的哈哈

```
// 完善后的 ProductListPage 设计
ProductListPage: {
  // 🎯 URL 参数（驱动页面状态）
  urlParams: {
    query: "",        // 搜索关键词
    category: "",     // 商品分类
    minPrice: "",     // 最低价格
    maxPrice: "",     // 最高价格
    sort: "default",  // 排序方式
    page: "1",        // 当前页码
    pageSize: "12"    // 每页数量
  },

  // 🎯 页面状态
  state: {
    products: [],           // 商品列表数据
    totalCount: 0,          // 商品总数（用于分页）
    isLoading: false,       // 加载状态
    error: null,            // 错误信息
    (可选)hasMore: true           // 是否有更多数据（用于无限滚动）
  },

  // 🎯 页面方法
  methods: {
    fetchProducts(),        // 获取商品数据
    handleFilterChange(),   // 处理筛选变化
    handleSortChange(),     // 处理排序变化
    handlePageChange(),     // 处理分页变化
    handleSearch()          // 处理搜索
  },

  // 🎯 子组件结构
  children: {

    // 2. 筛选区域
    FilterSection: {
      props: {
        // 当前筛选条件
        activeFilters: {
          category: urlParams.category,
          priceRange: {
            min: urlParams.minPrice,
            max: urlParams.maxPrice
          }
        },
        // 可用筛选选项
        filterOptions: {
          categories: [],    // 从API获取的分类列表
          priceRanges: [     // 预定义的价格区间
            { label: "0-100元", min: 0, max: 100 },
            { label: "100-500元", min: 100, max: 500 }
          ]
        },
        // 回调函数
        onFilterChange: methods.handleFilterChange,
        onFilterReset: () => methods.handleFilterChange({}) // 重置筛选
      },
      children: {
        CategoryFilter: {
          props: {
            categories: filterOptions.categories,
            selectedCategory: activeFilters.category,
            onCategoryChange: (category) => onFilterChange({ category })
          }
        },
        PriceFilter: {
          props: {
            priceRanges: filterOptions.priceRanges,
            selectedRange: activeFilters.priceRange,
            onPriceChange: (range) => onFilterChange({
              minPrice: range.min,
              maxPrice: range.max
            })
          }
        }
      }
    },

    // 3. 主要内容区域
    MainContent: {
      props: {
        layout: "grid", // 布局方式：grid | list
        isLoading: state.isLoading,
        isEmpty: state.products.length === 0
      },
      children: {
        // 3.1 工具栏（排序 + 布局切换 + 搜索）
        Toolbar: {
          props: {
            currentSort: urlParams.sort,
            currentLayout: "grid",
            searchQuery: urlParams.query,
            productCount: state.totalCount,
            onSortChange: methods.handleSortChange,
            onLayoutChange: (layout) => {/* 更新布局状态 */},
            onSearch: methods.handleSearch
          },
          children: {
            SortDropdown: {
              props: {
                options: [
                  { value: "default", label: "默认排序" },
                  { value: "price_asc", label: "价格从低到高" },
                  { value: "price_desc", label: "价格从高到低" },
                  { value: "newest", label: "最新上架" },
                  { value: "popular", label: "销量最高" }
                ],
                value: currentSort,
                onChange: onSortChange
              }
            },
            LayoutToggle: {
              props: {
                currentLayout: currentLayout,
                onLayoutChange: onLayoutChange
              }
            },
            SearchBox: {
              props: {
                placeholder: "在结果中搜索...",
                value: searchQuery,
                onSearch: onSearch
              }
            },
            ResultsCount: {
              props: {
                count: productCount,
                query: searchQuery
              }
            }
          }
        },

        // 3.2 商品展示区域
        DisplaySection: {
          props: {
            products: state.products,
            layout: currentLayout,
            isLoading: state.isLoading
          },
          children: {
            // 3.2.1 加载状态
            LoadingState: {
              condition: state.isLoading,
              props: {
                skeletonCount: 12
              }
            },

            // 3.2.2 空状态
            EmptyState: {
              condition: state.products.length === 0 && !state.isLoading,
              props: {
                title: urlParams.query ? `没有找到"${urlParams.query}"相关商品` : "暂无商品",
                description: "尝试调整筛选条件或搜索其他关键词",
                actionLabel: "浏览所有商品",
                onAction: () => methods.handleFilterChange({}) // 重置所有筛选
              }
            },

            // 3.2.3 错误状态
            ErrorState: {
              condition: state.error !== null,
              props: {
                error: state.error,
                onRetry: methods.fetchProducts
              }
            },

            // 3.2.4 商品网格
            ProductGrid: {
              condition: state.products.length > 0,
              props: {
                products: state.products,
                layout: currentLayout,
                onProductClick: (productId) => {/* 跳转到商品详情 */},
                （可选）onAddToCart: (product) => {/* 添加到购物车 */}
              },
              children: {
                ProductCard: {
                  props: {
                    product: {}, // 单个商品数据
                    layout: currentLayout,
                    (可选)onQuickView: (product) => {/* 快速预览 */},
                    （可选）onAddToCart: (product) => {/* 添加到购物车 */},
                  }
                }
              }
            },

            // 3.2.5 分页组件
            Pagination: {
              condition: state.products.length > 0,
              props: {
                currentPage: parseInt(urlParams.page),
                totalPages: Math.ceil(state.totalCount / parseInt(urlParams.pageSize)),
                pageSize: parseInt(urlParams.pageSize),
                onPageChange: methods.handlePageChange,
                onPageSizeChange: (newSize) => methods.handleFilterChange({
                  pageSize: newSize,
                  page: "1"
                })
              }
            },

            // 3.2.6 无限滚动（备选方案）
            InfiniteScroll: {
              condition: false, // 可以根据需求启用
              props: {
                hasMore: state.hasMore,
                onLoadMore: () => methods.handlePageChange(parseInt(urlParams.page) + 1)
              }
            }
          }
        }
      }
    },

    // 4. 侧边栏（可选）
    Sidebar: {
      props: {
        isSticky: true,
        content: {
          recentlyViewed: [],    // 最近浏览
          popularProducts: [],   // 热门商品
          promotions: []         // 促销信息
        }
      }
    }
  }
}
```

先复制过来免得翻来翻去

详情页业务逻辑分析

1. 首先回看导航栏，放在Layout里面，负责URL跳转等操作（搜索请求回调处理，有个问题，那么这个列表是不是放在Zustand里面可能更好）
2. 对于Page，里面应该有物品的详细状态，使用State即可
3. 轮播图和缩略图一样，都应该传入props
4. 相似商品推荐，也是展示，应该同时传入跳转回调
5. 信息与规格筛选，这里的信息应该是跟随商品一起传进来的，需要注意的是，里面有加入购物车的行为，点击后，应该使用zustand改变全局状态，这个与购物车相关的了。

Page：商品的详细信息，加入购物车的回调，链接跳转回调？（把跳转作为推荐物品本身的属性还是传入的props？）
主图轮播区：props，由Page传入
信息与规格选择区：信息，由Page传入，不过，对于像选中尺码和颜色，这些应该存在Page的state里面吧，而像购买数量，也放在Page，以及触发的回调，也应该在Page里

根据我对淘宝的观察，实际上，同一件商品，其实在修改规格等属性的时候，url也在发生变化。
也就是说，当我们修改规格时，其实也在跳转路由，信息也在变化，不过是选择性的变化，选择性的更新state

```
// 展示页组件
ProductPage:{
    urlParams:{
        productId,      // 当前的详细页的物品id
        skuid,          // 将不同规格的商品映射为不同的sku，更改价格库存等内容
    },
    state:{
        productInfo,
        showingImage，
        // isLoop,      // 本来打算加上循环轮播的，但是实际看到的淘宝详细页里面并没有，仔细想想也是
        count,          // 用于记录添加到购物车里的数量
        variant,        // 不同的规格
        stock,
        price,
    },
    method:{
        addCart,        // 将物品加入购物车
        fetchProduct,   // 获取物品，参数就是上面那个urlParams
    }，children:{
        imageSection:{
            condition:productInfo.images.length > 0
            props:productInfo
            children:{
                showingImageSection:{
                    props:showingImage
                },
                imagesList:{
                    props:productInfo.images
                }
            }
        },
        InfoPanel:{
            props:productInfo,variant,price,count,stock,addCart
        }
    }
}
```

## 数据结构设计(商品)

```
product:{
    id: string,
    name: string,
    properties: {
        color: ["red", "blue", "black"],
        size: ["S", "M", "L", "XL"]
    },
    sku:[
        {
            id:"123",
            properties:{color: "red", size: "S"},
            price: 100,
            stock: 100,
            imgage: "T-shirt-red-s.jpg"
        },{
            id:"456"
            properties:{color: "blue", size: "S"},
            price: 200,
            stock: 50,
            image: "T-shirt-blue-s.jpg"
        }
    ]
}
```
